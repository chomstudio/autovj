# オートVJソフト 企画書・MVP仕様書

作成日: 2026-06-27  
対象: Windows 11向けデスクトップアプリ  
ステータス: 企画・MVP仕様案

## 1. 企画概要

本企画は、DJが再生している音声をリアルタイムに解析し、ローカルフォルダ内にある対応動画を自動で同期再生する、完全オフライン対応のオートVJソフトを開発するものである。

ユーザーはVJ用PC本体を直接操作せず、同一PCまたはLAN内のスマートフォン・タブレット・別PCのブラウザから操作画面へアクセスする。映像出力はWindows 11上の専用フルスクリーン出力として行い、操作画面はローカルHTTPサーバー経由のHTML/CSS/JavaScriptで実装する。

初期MVPでは、DJ音声から曲を検出し、対応するメイン動画を同期再生することを最優先とする。重ね合わせ素材、BPM同期切替、複数レイヤー合成、認識不能時の高度な挙動は段階的に拡張する。

## 2. 開発方針

- 商用配布を前提とし、ライセンス上扱いやすい技術を優先する。
- Windows 11専用とし、Windows 10以前は対象外とする。
- C#/.NETをコア実装に採用する。
- GUIはネイティブUIではなく、ローカルWeb UIとして実装する。
- 動画標準形式はMP4/H.264/AACとする。
- 再生性能はFHD 30fpsを最低目標とする。
- 初期MVPでは確実に動く範囲を狭く定義し、認識精度や演出は実機検証で調整する。

## 3. 対象環境

### 3.1 対応OS

- Windows 11 64bit

### 3.2 対応動画形式

標準対応形式は以下とする。

- コンテナ: MP4
- 映像: H.264/AVC
- 音声: AAC
- 解像度: FHDを標準想定
- フレームレート: 30fpsを最低目標

H.265/HEVC、AV1、VP9、ProRes、10bit、4:2:2、4:4:4などは初期MVPでは標準対応対象外とする。

## 4. 技術構成

### 4.1 コア

- 言語: C#
- ランタイム: .NET
- 配布方式: self-contained / win-x64を基本方針とする
- 処理内容:
  - 音声入力
  - 音声解析
  - 音声指紋照合
  - 動画制御
  - Web API
  - 設定管理
  - DB管理

### 4.2 操作GUI

- ASP.NET CoreによるローカルHTTPサーバー
- HTML/CSS/プレーンJavaScriptによる操作画面
- ブラウザ内蔵は行わない
- 起動時に操作URLを表示する
- LAN公開時はスマートフォン等からの操作を想定する

### 4.3 映像出力

- Windows標準機能で可能な範囲を優先する
- Direct3Dシェーダーを使ったほうが再生・合成が安定する場合は採用する
- 初期MVPではメイン動画 + 重ね合わせ素材1レイヤーを目標とする
- 将来的には重ね合わせ素材2〜3レイヤーまで拡張する

### 4.4 外部ツール

FFmpegはLGPLビルドを同梱し、必須依存とする。

主な用途は以下を想定する。

- メディア情報の取得
- 音声抽出
- 事前解析補助
- 形式チェック
- 将来的な素材変換支援

動画再生そのものは、可能な限りWindows標準機能およびDirect3D系の出力を優先する。FFmpegを再生デコーダーとして直接利用するかどうかは、実装検証後に判断する。

FFmpeg同梱時は、以下を必須とする。

- LGPLビルドを採用する
- GPL有効ビルド、nonfreeビルドは使用しない
- FFmpegのライセンス文書を同梱する
- サードパーティライセンス一覧をインストーラーおよび配布物に含める

## 5. 音声入力仕様

### 5.1 標準対応

以下の音声入力を初期MVPから必須対応とする。

- WASAPIループバック
- LINE入力

WASAPIループバックは、PC上で再生されているDJ音声を取得する用途を想定する。LINE入力は、DJミキサーやオーディオインターフェースから直接音声を入力する用途を想定する。

### 5.2 対応しないもの

初期MVPではASIOには対応しない。

理由は以下の通りである。

- 即時性をASIO必須レベルまでは求めない
- ドライバ導入がユーザー負担になる
- 初期MVPの検証範囲が広がりすぎる

## 6. 音声認識・同期仕様

### 6.1 基本動作

1. 入力音声をリアルタイムに取得する。
2. 短い時間窓ごとに音声指紋または照合用特徴量を生成する。
3. ローカルDBに登録された動画・曲情報と照合する。
4. 曲候補、信頼度、推定再生位置を算出する。
5. 十分な信頼度に達した場合、対応動画を推定位置へ同期して再生する。

### 6.2 テンポ・ピッチ変更への考え方

テンポ変更やピッチ変更への許容範囲を広げるほど、候補数が増え、確定まで時間がかかりやすくなる。また、誤検出やCPU負荷も増えやすい。

そのため、初期段階では固定の数値仕様を厳密に決めず、MVP実装後の実機検証で以下を調整する。

- 検出に必要な秒数
- テンポ変更の許容幅
- ピッチ変更の許容幅
- ミックス中の候補切替条件
- スキップ検出時の再同期条件

### 6.3 ミックス時の挙動

DJの曲つなぎでは複数音声が重なる可能性がある。この場合、あとから入ってきた曲を優先して動画を切り替える方針とする。

ただし、初期MVPでは完全な判定を目標とせず、以下の情報から段階的に推定する。

- 新しい曲候補の信頼度上昇
- 現在曲候補の信頼度低下
- 推定再生位置の連続性
- 一定時間内の候補安定度

## 7. 映像再生・演出仕様

### 7.1 MVP範囲

初期MVPでは以下を実装対象とする。

- 検出曲に対応するメイン動画の再生
- 推定再生位置への同期
- 再検出時の再同期
- メイン動画 + 重ね合わせ素材1レイヤー
- FHD 30fpsを最低目標とした再生

### 7.2 将来拡張

将来的に以下を実装する。

- 重ね合わせ素材2〜3レイヤー
- BPMに合わせた素材切替
- 数秒ごとのランダム素材切替
- 加算、乗算、減算などのブレンド効果
- 静止画素材対応
- 認識不能時の汎用素材再生
- 複数動画候補のランダム選択

## 8. 素材登録仕様

### 8.1 MVP範囲

初期MVPでは、動画ファイルから音声情報を事前解析し、曲検出用DBへ登録する。

登録対象は以下とする。

- 曲に対応するメイン動画
- 重ね合わせ用素材動画
- 将来的な汎用素材動画

### 8.2 手動紐づけ

手動で曲と動画を紐づける機能は、初期MVPでは必須としない。実装は後続フェーズで検討する。

## 9. Web UI仕様

### 9.1 基本画面

初期MVPのWeb UIでは、以下の画面を想定する。

- ダッシュボード
- 入力デバイス選択
- 検出状態表示
- 再生中動画表示
- 素材フォルダ設定
- DB登録・再解析
- ネットワーク設定
- ログ表示

### 9.2 ネットワーク設定

デフォルトでは `127.0.0.1` のみで待ち受ける。

LAN操作を有効にした場合のみ、LAN内の端末からのアクセスを許可する。グローバル回線からのアクセスは想定せず、ブロックする方針とする。

### 9.3 認証

実行ごとにランダムな4桁程度の数値PINを生成し、起動画面またはコンソールに表示する。

設定ファイルに固定PINを指定した場合は、固定PINを使用できるようにする。

初期方針は以下とする。

- デフォルト: 起動ごとのランダムPIN
- 設定可能: 固定PIN
- LAN公開時: PIN認証必須
- グローバル公開: 非対応
- HTTPS: 非対応

## 10. 設定ファイル

設定ファイル形式はYAMLとする。

設定例:

```yaml
server:
  host: "127.0.0.1"
  port: 0
  allow_lan: false
  fixed_pin: null

media:
  main_video_dir: "D:/AutoVJ/main"
  overlay_dir: "D:/AutoVJ/overlay"
  fallback_dir: "D:/AutoVJ/fallback"

audio:
  preferred_input: "wasapi_loopback"
  sample_rate: 48000
  channels: 2

playback:
  target_fps: 30
  output_display: 1
  fullscreen: true
  overlay_layers: 1

ffmpeg:
  bundled: true
  path: "tools/ffmpeg/ffmpeg.exe"
```

## 11. DB仕様

### 11.1 採用DB

SQLiteを採用する。

理由は以下の通りである。

- 単一ファイルで扱いやすい
- 商用配布と相性がよい
- バックアップしやすい
- ローカルアプリ用途に十分な性能がある
- SQLで索引や検索条件を明示できる

### 11.2 .NET側アクセス

以下を基本方針とする。

- Microsoft.Data.Sqliteを使用する
- 必要に応じてDapperを併用する
- 音声指紋照合など速度が必要な部分はSQLを直接制御する
- EF Coreは初期MVPでは必須としない

### 11.3 想定テーブル

- `tracks`: 曲・動画の基本情報
- `media_files`: ファイルパス、形式、長さ、更新日時、ハッシュ
- `fingerprints`: 音声指紋データ
- `matches_log`: 検出履歴、信頼度、推定位置
- `settings`: UIから変更する設定
- `manual_links`: 将来の手動紐づけ用

## 12. インストーラー仕様

### 12.1 採用候補

初期MVPではInno Setupを採用する。

理由は以下の通りである。

- 作成が比較的簡単
- 単一EXEのインストーラーを作りやすい
- 商用配布に使いやすい
- MVP段階で必要な機能を満たしやすい

### 12.2 配布内容

インストーラーには以下を含める。

- アプリ本体
- .NET self-contained runtime
- Web UI静的ファイル
- SQLite初期DBまたはDB作成処理
- FFmpeg LGPLビルド
- 設定ファイルテンプレート
- ライセンス文書
- サードパーティライセンス一覧

### 12.3 将来検討

企業向け配布や厳密なMSI運用が必要になった場合はWiX Toolsetを検討する。Microsoft Store配布やMSIX固有のメリットが必要になった場合はMSIXを検討する。

## 13. ライセンス方針

商用配布を前提とし、以下の方針を採用する。

- MIT、BSD、Apache-2.0、Public Domain系を優先する
- LGPLは条件を満たせる範囲で採用する
- GPL、AGPL、nonfree依存は原則採用しない
- FFmpegはLGPLビルドのみ採用する
- すべてのサードパーティライセンスを配布物に同梱する

初期候補:

- .NET / ASP.NET Core
- SoundFingerprinting
- NAudio
- Math.NET Numerics
- Microsoft.Data.Sqlite
- Dapper
- SQLite
- FFmpeg LGPLビルド

## 14. MVPスコープ

初期MVPで実装する機能は以下とする。

- Windows 11対応
- ローカルWeb UI
- `127.0.0.1` 操作
- 設定によるLAN操作
- PIN認証
- WASAPIループバック入力
- LINE入力
- 素材フォルダ登録
- 動画音声の事前解析
- SQLite DB登録
- 曲検出
- メイン動画の同期再生
- メイン動画 + 重ね合わせ素材1レイヤー
- FHD 30fps再生目標
- FFmpeg LGPLビルド同梱
- Inno Setupインストーラー

## 15. MVP対象外

初期MVPでは以下を対象外とする。

- Windows 10対応
- macOS/Linux対応
- ASIO対応
- HTTPS対応
- グローバル回線からの操作
- ブラウザ内蔵
- 手動紐づけUI
- 2〜3枚以上の重ね合わせ
- 高度なBPM同期素材切替
- HEVC/AV1などの標準対応
- 商用VJソフト級の完全なミックス検出

## 16. 検証項目

開発初期に以下を検証する。

- Windows 11環境でのWASAPIループバック取得
- LINE入力デバイスの列挙と選択
- H.264/AAC MP4の安定再生
- Direct3Dシェーダー合成の安定性
- メイン動画 + 1レイヤーでFHD 30fpsを維持できるか
- SoundFingerprinting等による曲検出速度
- テンポ変更時の検出時間と誤検出率
- ピッチ変更時の検出時間と誤検出率
- ミックス中の後入り曲推定
- LAN公開時のPIN認証
- FFmpeg LGPLビルド同梱時のライセンス文書整備
- Inno Setupでのself-contained配布

## 17. 開発フェーズ案

### フェーズ1: 技術検証

- 音声入力検証
- MP4再生検証
- 音声指紋DB検証
- Web UI + ローカルAPI検証
- FFmpeg同梱検証

### フェーズ2: MVP実装

- アプリ構成確定
- SQLite DB実装
- 素材登録
- 曲検出
- 動画同期再生
- Web UI基本画面
- PIN認証
- Inno Setupインストーラー

### フェーズ3: 演出拡張

- 重ね合わせ素材切替
- ブレンド効果
- BPM同期切替
- 静止画素材対応
- 汎用素材再生

### フェーズ4: 品質改善

- 認識精度調整
- ミックス中判定改善
- パフォーマンス最適化
- ログ改善
- エラー診断
- ライセンス文書整備

## 18. 未決・調整予定事項

以下はMVP実装後、実機検証を通じて調整する。

- テンポ変更の許容幅
- ピッチ変更の許容幅
- 検出確定に必要な秒数
- ミックス時の切替条件
- 認識不能時の汎用素材切替条件
- 素材切替間隔
- BPM同期切替の精度
- 重ね合わせ効果の種類
- 手動紐づけUIの優先度

