# auto vj 指示書

## この指示書について

本ファイルは、チャットプロンプトのみでは説明しきれない内容を長文テキストで記述するための場所である。
Codexは本ファイルの内容を読み、次にすべき作業内容を理解すること。

基本的にこのファイルは私（ユーザー）が記述するもので、特に指示がない限りCodexから編集しないこと。

このファイルの内容（特に実装済の項目）は私の判断で内容が削除される。
よっていつまでも残っているとは限らない。
実装済の仕様は早い段階で「プロトタイプ仕様書」に書き写すこと。

## プロトタイプ仕様書について

`auto_vj_project_spec.md` は、本プロジェクトが最終的にめざす内容の仕様書である。
それに対し、`prototype_spec.md` は、現在進行形の仕様書である。これを「プロトタイプ仕様書」とする。

実装作業と平行してこのファイルを更新し、このプロジェクトが今どうなっているかが把握できる形にまとめて欲しい。
新しいチャットを始めた際は、このプロトタイプ仕様書を参照すれば、ただちに作業の続きに入れる形が望ましい。

---

## 次の指示

### ちょっとした機能追加

#### 再生位置をずらす

オプションで「再生位置をずらす」機能を追加してください。

現在、オーディオデバイス経由で再生して認識せると、再生位置がだいたい１秒ほどすれます。
もちろん認識速度が早まるとよいですが、限界もありますので、手動で固定秒数（単位はミリ秒）あらかじめずらせるようにしたいです。
つまり、1秒ずれるのなら、1秒前に再生位置を移動させるようにすれば結果的にピッタリ合うという寸法です。
ずらす秒数は設定画面から設定できるようにしてください。
また、数字はマイナス秒を許容するようにしてください。つまり -1000(ミリ秒)を指定すると、1秒遅らせます。

ちなみにループバックで認識させたときはズレはもっと短く、0.5秒ほどでした。ズレはデバイスによって変わるようです。
ずらす秒数はデバイスに紐づけておいて、デバイスが変わると自動で適用されるようになるとなお良いです。

#### LAN公開

起動オプションでLAN内の他の機器から各ページにアクセスできるようにしてください。
ポート番号も起動時に指定できるようにしてください。
PIN番号（4桁の数字）は、設定ファイルでの指定を基本とし、起動オプションで指定して一時的に上書き設定できるようにしてください（オプション指定しなければ設定ファイル通りに動く）

PIN番号はウェルカムページに入力欄を表示し、PINが通れば設定ページ／再生ページを開けるようにします。
直接設定ページ／再生ページのURLにアクセスした場合は弾くようにします。

## メモ：実装予定のうち優先度が低いもの

- テンポとピッチを同時変更する入力への対応
- 専用Windows映像出力（アプリ内映像合成）、仮想カメラデバイス出力
- 配布用インストーラー
