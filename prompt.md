# auto vj 指示書

## この指示書について

本ファイルは、チャットプロンプトのみでは説明しきれない内容を長文テキストで記述するための場所である。
Codexは本ファイルの内容を読み、次にすべき作業内容を理解すること。

基本的にこのファイルは私（ユーザー）が記述するもので、特に指示がない限りCodexから編集しないこと。

このファイルの内容（特に実装済の項目）は私の判断で内容が削除される。
よっていつまでも残っているとは限らない。
実装済の仕様は早い段階で「プロトタイプ仕様書」に書き写すこと。

## プロトタイプ仕様書について

`auto_vj_project_spec.md` は、本プロジェクトが最終的にめざす内容の仕様書である。
それに対し、`prototype_spec.md` は、現在進行形の仕様書である。これを「プロトタイプ仕様書」とする。

実装作業と平行してこのファイルを更新し、このプロジェクトが今どうなっているかが把握できる形にまとめて欲しい。
新しいチャットを始めた際は、このプロトタイプ仕様書を参照すれば、ただちに作業の続きに入れる形が望ましい。

---

## 次の指示

**WASAPIループバック入力に対応させてください。**

- これは想定しているメインの使い方ではなく、ユーザーがこのアプリが正しく動いているか（検出できるか）の検証、および手持ちの音源と再生される動画のペアを確認を、外部機器を必要とせずに行うための機能になります。

**新規に、再生ページと設定ページを作成しください。**

- 再生プレイヤーのみ表示するページ（例えば `/output`）、設定のみできるページ （例えば`/setting`）をそれぞれ新しく作ってください。
- 設定できるページの内容は再生プレイヤー以外のすべての表示内容を引っ越してきてください。これはスクロールバーが出ても問題ありません。
- 設定ページで変更した内容（入力デバイス選択、受付停止など）は即座に再生ページにも反映できるようにしてください。
- 再生ページ上でランダムに適用している重ね合わせ効果はサーバー側で指定するようにしてください。複数のタブで同じ再生ページを開いても、常に同じ演出がされるようにしてください。
- 設定の不整合が発生するおそれがあるため、現在の「再生＋設定ページ」は廃止し、再生ページと設定ページを新規タブで開くのみのウェルカムページの役割をもたせてください。

### 今後の予定に対しての再検討

現在未実装とされている機能について、必要性を再度検討してみました。

#### テンポとピッチを同時変更する入力への対応

対応できるとうれしいのは確かですが、最優先ではありません。
以前言ったように、アナログ・レコード派のDJは本ソフトのメインターゲットではないからです。

#### ミックス時の後入り曲判定

これは「２つの曲が重なった場合は、あとからかけたほうを優先して動画再生する」という当初の要望のことだと思いますが、
現状ではそもそも２つの曲が重なった状態だと認識率が落ちるため、どちらがあとからかけた曲なのか？を判定するのは難しいと思います。
今まで通り、認識率が下がった状態では現在再生中の動画の再生をキープし、判明した時点で切り替える方式で十分です。
結論として、現状のままで問題ありません。

#### BPM手動修正UI

DJは本番中はDJに集中しており、このソフトは基本的に起動時を除いてほったらしにされる想定です。
DJは本番中に「流れている動画のBPMを確認して、必要に応じて手作業で修正する」ような暇はありません。よって、この機能があったとしても使われません。
そもそもこのソフトを使っている時点で、DJは動画の多少の再生ズレを許容するとします。
許容できないとすれば「あまりにもズレている」ときです。それは、例えばBPM180の曲がBPM90と認識されてしまった、ような状態です。さすがにそうなったら直したいと思うでしょう。
なので、以下を確認してください。

- BPM倍率、再生倍率にリミットをかける。たとえば 0.8～1.2倍の範囲など。
  - 設定画面で変更できると良い。
- 設定画面であらかじめBPM範囲を決めておき、その範囲外のBPMが検出されたときは、倍テン（BPMが2倍）、半テン（BPMが半分）されていることを疑い、範囲内に収める
  - デフォルトでは BPM100以上200未満にします。これは私が個人的に使っているBPM範囲であり、ユーザーによってかなり変わります。
  - 具体的には、BPM90の曲が来たら、倍にして180にすることで、ユーザーが指定したBPM範囲内に収めます。逆の場合も同様です。

これらが実装されれば、BPM手動修正の実装は不要です。

#### 専用Windows映像出力

いまのところブラウザ上の再生でもそれほどストレスを感じていません。
DirectXを使った高度な再生は、今後の「動画素材の重ね合わせ」の実装後、気になるほどパフォーマンスが落ちたときに再考しましょう。

#### マルチモニター対応

そもそもWebアプリでそれが可能なのか？の話は置いておいて、現場での話をします。
VJソフトは一般的にマルチモニター対応が必須です。それは、プロジェクタに移す画面と、ユーザーが操作する画面を分離する必要があるからです。
しかしこのアプリは一般的なVJソフトとは以下が違っています。

- 基本的に本番中にユーザーが操作する必要がない
- ブラウザはそもそもマルチウィンドウである
- 必要なら別タブ、あるいは（「LAN公開」機能が実装できれば）スマホから設定を変えられる

このソフト自身がマルチモニターを考慮する必要性はほぼないでしょう。

#### LAN公開とPIN認証

検討中

#### 配布用インストーラー

検討中

#### 仮想カメラデバイス対応

仮想カメラデバイス対応は野望のひとつではありますが、
アプリ内部で映像を合成するロジックを組むのはややヘビーなため、
いまのところはブラウザによる合成に任せ、必要があればブラウザを外部のアプリ（例えばOBS）を通してキャプチャ、
のほうがよさそうです。
仮想カメラデバイス対応は後回しにします。
