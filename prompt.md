# auto vj 指示書

## この指示書について

本ファイルは、チャットプロンプトのみでは説明しきれない内容を
長文テキストで記述するための場所である。

Codexは本ファイルの内容を読み、次にすべき作業内容を理解すること。

基本的にこのファイルは私（ユーザー）が記述するもので、
特に指示がない限りCodexから編集しないこと。

このファイルの内容（特に、実装済の項目）は私の判断で内容が削除される。
よっていつまでも残っているは限らない。
実装済の仕様は早い段階で「プロトタイプ仕様書」に書き写すこと。

## プロトタイプ仕様書について

`prototype_spec.md` を用意した。（現在は空）
`auto_vj_project_spec.md` が、本プロジェクトが最終的にめざす内容が記述されているファイルに対して、
このファイルは「現在進行形」の仕様書とする。

実装作業と平行してこのファイルを更新し、このプロジェクトが今どうなっているかが把握できる形にまとめて欲しい。
新しいチャットを始めた際は、このプロトタイプ仕様書を参照すれば、ただちに作業の続きに入れる形が望ましい。

## 現在の問題点

現在が最小構成の M0 であることも踏まえ、以下の問題点は今後開発の進行に合わせて適当なタイミングで修正してほしい。

- サーバー起動時には自動でブラウザが開くようにすると良いだろう。
- HTMLで作成された動画プレイヤーは現在の状態では動画の終端で止まってしまうため、ループ状態で開始するべきだ。そして、ユーザーがそれ（再生とループ状態）を解除できないほうがのぞましい。
- アプリ開始直後、および検出できない（曲が特定できない）状態が一定時間以上続いた場合は、「汎用の動画」を再生開始しループさせておきたい。画面が黒のままだったり、映像が停止したままなのは好ましくない。
  - 新しく `material_videos` フォルダを作成し、中に `common_movie.mp4` を置いた（長さ10秒、無音）。とりあえずアイドル時はこれをループ再生してほしい。
- 音声入力信号が入っているかのインジケーターが必要だ。
- 入力デバイスは任意のタイミングで切り替えられるべきだ。
- 検出時間に８秒はかなり長い。せめて４秒程度には抑えられないか。（もちろん短ければ短いほどよい）
- 動画の切り替えに簡単なエフェクトが必要だ。まずはクロスフェードでも良い。将来的には凝った演出も加えたい。
- 現在は再生ページ上からフォルダ内動画の再解析ができるが、動画のデータベース化アプリは再生の裏で動かせるように別アプリにしたほうがよさそうだ。可能なら現在の動画再生を止めずに新規動画をデータベースに追加できるともっと良い。
