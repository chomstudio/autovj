# AutoVJ プロトタイプ仕様書

更新日: 2026-07-16
現在の段階: M0.4実装完了・M0.5のキーロック対応テンポ検出に向けた作業方針を確定

## 1. 本書の位置付け

本書は、現在動作するプロトタイプの実装内容を記録する。最終目標は `auto_vj_project_spec.md`、ユーザーからの次の作業指示は `prompt.md` を正とする。

## 2. M0の目的

企画書のMVP全体を一度に実装せず、次の縦方向の動作を先に成立させる。

1. MP4の音声トラックから動画単位の指紋を登録する。
2. LINE入力をリアルタイム取得する。
3. 入力から登録曲と再生位置を推定する。
4. 対応動画をブラウザで推定位置から再生する。
5. 再検出した位置と動画が2秒を超えてずれた場合に再同期する。

M0.1では、映像が黒画面や停止状態にならない連続再生、入力信号の可視化、実行中のデバイス切り替えを追加する。

M0.2では、検出窓を4秒へ短縮し、弱い候補の連続確認、無音ゲート、新しい位置検出時だけシークする再同期制御を追加する。

M0.3では、2枚の動画レイヤーを用いたクロスフェードと、CSSブレンドモードによるランダムな重ね合わせ演出を追加する。

M0.4では、動画解析を独立した `AutoVJ.Catalog` へ分離し、再生アプリを止めずに新規・更新動画を差分登録できる構成へ変更する。

## 3. 実装済み機能

### 3.1 設定

- `config.yaml` を使用する。
- 外部YAMLライブラリは使用せず、M0で既知のキーだけを読み取る。
- Web UIは `127.0.0.1:5180` で待ち受ける。
- 既定入力デバイスは `LINE (Yamaha AG03MK2)` とする。
- `server.auto_open_browser` が有効な場合、待受開始後にOS既定ブラウザで操作画面を開く。
- 自動試験時は環境変数 `AUTOVJ_NO_BROWSER=1` でブラウザ起動だけを抑止できる。
- 汎用動画は実在する `material-videos/common_movie.mp4` を使用する。

### 3.2 素材登録

- 登録対象は `main-videos` の `*.mp4` とする。
- 解析は独立したコンソールアプリ `AutoVJ.Catalog` の `scan` コマンドで実行する。引数省略時も `scan` とする。
- 開発環境では `scan-videos.cmd` をダブルクリックして同じ処理を起動できる。
- ファイルサイズと最終更新日時が一致する登録済み動画は解析を省略する。
- 新規・更新動画は1本ずつ解析し、完成した指紋からDBへ反映する。
- 解析失敗時は既存のDBレコードを維持し、ほかの動画の処理を続行する。
- 同じDBを対象とする解析アプリの多重起動はロックファイルで拒否する。
- 各MP4の音声トラックから指紋を生成し、そのMP4自身と同じDBレコードへ保存する。
- `input-audio` の `*.mp3` と `*.wav` は登録元にせず、自己診断または実機入力へ流す検出確認用とする。
- MP3とMP4のファイル名や再生時間による対応付けは行わない。
- 現在の登録動画は次のとおり。

| 登録動画 | 動画長 |
|---|---:|
| `video-e.mp4` | 228.500秒 |
| `video-k.mp4` | 245.967秒 |
| `video-m.mp4` | 225.300秒 |
| `video-o.mp4` | 281.578秒 |
| `video-s.mp4` | 196.196秒 |

### 3.3 音声解析・指紋

- FFmpegを外部プロセスとして使用する。
- MP4の音声トラックを11,025Hz、モノラル、32bit float PCMへ変換する。
- 2,048サンプルの窓、1,024サンプルのホップでFFTを行う。
- 4周波数帯の最大ピークを量子化した指紋列を生成する。
- 最新4秒の入力を1秒ごとに全登録曲へ照合する。
- 信頼度0.62以上の候補は即時確定する。
- 信頼度0.55以上0.62未満の候補は、同じ動画が2回連続した場合に確定する。
- 入力窓全体が-48dB未満の場合は無音として照合しない。
- テンポ変更・ピッチ変更・複数曲ミックスへの耐性は未調整とする。

### 3.4 音声入力

- NAudioの `WasapiCapture` を使用する。
- Windowsの有効な録音エンドポイントを列挙できる。
- 設定名と大文字・小文字を区別せず完全一致し、見つからない場合は部分一致する。
- 16bit、24bit、32bit PCMおよび一般的な32bit float入力をモノラル化する。
- 簡易ダウンサンプリングで指紋処理用サンプルレートへ変換する。
- 入力PCMのRMSをdBへ変換し、-60dBから0dBを0から100%のメーターとして平滑化表示する。
- Web UIから任意の有効な録音エンドポイントを選択できる。
- 監視中にデバイスを変更した場合、旧デバイスを解放して新デバイスで自動的に監視を再開する。

### 3.5 DB

- SQLiteを使用する。
- DBは `data/autovj.db` に生成する。
- `tracks` テーブルに動画パス、長さ、動画音声由来の指紋を保存する。
- `PRAGMA user_version = 3` とし、ファイルサイズと最終更新日時を保存する。
- SQLiteはWALモードと5秒のビジー待機を使用し、再生アプリの読み取りと解析アプリの短い書き込みを両立する。
- 動画パスを一意キーとする `UPSERT` で既存IDを維持し、フォルダから消えた動画だけを削除する。
- 音声デコードと指紋生成はDBトランザクション外で行う。

### 3.6 Web UI・動画出力

- ASP.NET Coreで静的HTML/CSS/プレーンJavaScriptとAPIを配信する。
- 検出曲、信頼度、推定位置、入力状態を表示する。
- 音声指紋を登録済みの動画一覧を表示する。
- 入力開始、停止、映像領域の全画面表示を操作できる。
- 素材再解析ボタンと再解析APIは廃止し、解析は `AutoVJ.Catalog` だけが担当する。
- 登録動画一覧は5秒ごとに再取得し、外部解析結果を再生停止なしで反映する。
- MP4配信はHTTP Range要求に対応する。
- 動画はミュート状態で再生する。
- 動画要素には操作コントロールを表示せず、ミュートとループを常に強制する。
- 起動直後は `common_movie.mp4` を自動再生する。
- 曲未検出が10秒以上続いた場合は汎用動画へ戻る。
- 入力停止時は検出状態を消去し、汎用動画へ戻る。
- 曲が変わったときに動画を読み込み、推定位置へ移動して再生する。
- 同じ曲の再検出位置と再生位置の差が2秒を超えた場合に再同期する。
- 検出成功ごとに `matchRevision` を増加させ、Web UIは新しい番号を受信したときだけ再同期を判断する。
- 未検出や弱い候補の確認中は `matchRevision` と直前の再生位置を維持し、同じ区間へ繰り返しシークしない。
- 未検出が10秒未満なら直前の動画をそのまま進め、10秒に達した場合だけ汎用動画へ戻る。
- 動画出力は2枚のHTML Videoを絶対配置で重ねた構成とする。
- 起動直後の最初の汎用動画だけは即時表示し、それ以降の全ソース切り替えにクロスフェードを適用する。
- 既定のクロスフェード時間は1,200ミリ秒とする。
- 通常クロスフェードでは、旧動画をフェードアウトしながら新動画をフェードインする。
- ブレンド演出が有効な場合、新動画に `mix-blend-mode` を一時適用し、切り替え完了後は `normal` へ戻す。
- 既定候補は `screen`、`multiply`、`overlay`、`soft-light`、`difference` とする。
- ランダム選択を無効にした場合は候補一覧の先頭を使用する。
- ブレンド演出を無効にした場合も、`normal` による通常クロスフェードは行う。
- 切り替え完了後は旧レイヤーの `src` を削除して読み込みを解放する。
- 切り替え後の再生状態は新しいアクティブレイヤーから取得し、ソース解放済みの旧レイヤーに対して `play()` を待たない。
- 動画メタデータの読み込みは10秒でタイムアウトし、状態ポーリングが待機したままにならないようにする。
- Web UIには直近のブレンドモードと切り替え秒数を表示する。

## 4. API

| メソッド | パス | 内容 |
|---|---|---|
| GET | `/api/status` | 最新の入力・検出状態 |
| GET | `/api/client-config` | Web再生用の公開設定 |
| GET | `/api/tracks` | 指紋登録済み動画一覧 |
| GET | `/api/audio/devices` | 有効な録音デバイス一覧 |
| POST | `/api/capture/start` | 指定または選択中デバイスの監視開始 |
| POST | `/api/capture/device` | 入力デバイス変更と必要時の監視再開 |
| POST | `/api/capture/stop` | 入力監視停止 |
| GET | `/api/media/{id}` | Range対応MP4配信 |
| GET | `/api/material/common` | Range対応の汎用動画配信 |

環境変数 `AUTOVJ_ENABLE_TEST_API=1` のときだけ、次のUI回帰試験APIと操作ボタンを有効にする。通常起動では公開しない。

| メソッド | パス | 内容 |
|---|---|---|
| POST | `/api/test/match/{id}` | 指定動画を疑似検出して切り替える |
| POST | `/api/test/fallback` | 汎用動画へ戻す |

## 5. 検証結果

2026-07-15に次を確認した。

- `dotnet build --no-restore`: 警告0件、エラー0件。
- MP4音声から指紋を作成後、確認用 `moca_elementary_240829.mp3` から `video-e` を信頼度69.4%で検出。
- MP4音声から指紋を作成後、確認用 `rikka_morote_210303.mp3` から `video-o` を信頼度72.9%で検出。
- 4秒自己診断で `moca_elementary_240829.mp3` から `video-e` を信頼度69.6%で検出。
- 4秒自己診断で `rikka_morote_210303.mp3` から `video-o` を信頼度71.4%で検出。
- 登録動画5本を候補とした4秒ベンチマークで、2曲×4位置のうち即時確定6件、連続確認込み8件すべて成功。
- 4秒ベンチマークの最小一致度は57.7%と60.7%、平均は76.5%と80.5%。
- デジタル無音の最大一致度は48.8%で、弱候補しきい値55%を下回る。
- 未検出状態では `matchRevision` を維持し、新しい位置検出時だけ増加することを自己診断で確認。
- ローカルWeb UIに登録動画5本が表示され、M0.2のJavaScriptエラーがないことを確認。
- クライアント設定APIからクロスフェード1,200ミリ秒、ランダム選択有効、ブレンド候補5種類を取得できる。
- 疑似検出による切り替えで `difference`、`multiply`、`soft-light` がランダムに選択され、Web UIへ表示されることを確認。
- 切り替え後は新動画が不透明度1の前面レイヤーとなり、旧動画の `src` 属性が削除されることを確認。
- 汎用動画、検出動画、別の検出動画を連続して切り替えた後も状態更新と操作が継続し、ブラウザーエラーがないことを確認。
- 再生アプリ稼働中に既存5本を解析し、動画ID `3` から `7` を維持したまま更新できることを確認。
- 直後の再走査では5本すべてが変更なしとなり、指紋生成を省略することを確認。
- 隔離DBで新規1本の追加、変更1本の更新、削除1本の除去、多重起動時の終了コード3を確認。
- Web UIから素材再解析ボタンがなくなり、登録動画5本の定期更新とJavaScriptエラーなしを確認。
- ローカルHTTP画面に登録素材2件が表示される。
- `LINE (Yamaha AG03MK2)` の監視開始・停止に成功する。
- MP4のRange要求に `206 Partial Content`、1,024バイトで応答する。
- `common_movie.mp4` がH.264/AAC、1920×1080、30fps、10秒であることを確認。
- 汎用動画のRange要求に `206 Partial Content`、1,024バイトで応答する。
- 汎用動画が10秒の終端を越えても `loop=true`、`paused=false` のまま再生を継続する。
- AG03MK2入力から信号レベル値を取得できる。
- 監視中にAG03MK2からステレオミキサーへ切り替え、さらにAG03MK2へ戻しても監視状態を維持する。

自己診断は `input-audio` の途中4秒を、MP4音声由来の指紋へ照合する。エンコード差を含む検出は確認済みだが、実際のLINE経路によるノイズ、音量、遅延を含む認識精度はまだ未検証である。

## 6. 依存関係と配布上の注意

- .NET 10 / ASP.NET Core
- NAudio 2.2.1
- Microsoft.Data.Sqlite.Core 10.0.10
- SQLitePCLRaw.bundle_e_sqlite3 3.0.3
- FFmpeg / ffprobe

開発PCのPATH上にあるFFmpegはGPL有効ビルドであり、配布物には使用しない。商用配布前にLGPLビルドへの差し替え、ライセンス文書、サードパーティ通知を整備する。

## 7. 未実装

- WASAPIループバック入力
- キーロックを維持したテンポ変更への本格対応
- テンポ倍率に合わせた動画再生速度の追従
- ミックス時の後入り曲判定
- Windows専用フルスクリーン映像出力
- Direct3D再生・合成
- 重ね合わせ素材1レイヤー
- LAN公開とPIN認証
- 設定画面とログ画面
- self-contained配布
- LGPL版FFmpeg同梱
- Inno Setupインストーラー
- 解析アプリの `watch` モード

## 8. 次の開発工程: M0.5 キーロック対応テンポ検出

### 8.1 対象とするDJ運用

- ターゲットユーザーはキーロックを常時有効にし、ピッチを維持したままBPMを変更するDJとする。
- テンポ上昇は `+10%` から `+15%` を通常範囲とし、`+17%` 前後までを追加目標とする。
- テンポ下降は優先度を下げ、`-7%` 前後を実用範囲、`-10%` を試験上限とする。
- テンポとピッチが同時に変化するアナログレコード型の速度変更は低優先度とし、M0.5の必須要件に含めない。
- 入力音源は44.1kHzまたは48kHz、16bitまたは24bitのWAV、もしくは196kbps以上のMP3を想定する。
- 低ビットレート音源、8bit音源、極端なノイズや音質劣化への最適化は行わない。
- ハイパス／ローパスフィルター適用中の継続検出は必須としない。フィルター解除後に素早く新しい曲を確定できることを重視する。
- フィルターやMIXで一時的に検出できない間は、現在の動画を維持して不要な汎用動画切り替えを避ける。

### 8.2 M0.5a: 変換音源ベンチマーク

最初に本番検出ロジックを変更せず、現在方式を基準としてテンポ耐性を測定できる自動ベンチマークを追加する。

- 登録動画の音声から、FFmpegでピッチを維持したテンポ変更クエリを一時生成する。
- 必須の試験倍率は `0.90`、`0.93`、`0.95`、`1.00`、`1.05`、`1.10`、`1.15`、`1.17` とする。
- 各登録曲の複数位置から4秒、6秒、8秒のクエリを作る。
- ハイパス、ローパス、フィルター解除後のクエリも別枠で測定する。
- テンポとピッチが同時に変わる速度変更は、比較用の低優先試験として少数だけ用意する。
- 正解曲率、誤検出数、未検出数、推定位置誤差、確定までの時間、処理時間を記録する。
- 結果は方式名、指紋バージョン、倍率、変換条件ごとに比較できる形式で出力する。
- 通常速度 `1.00` では、現在成功している検出結果を悪化させないことを回帰条件とする。

### 8.3 M0.5b: 検出器の交換可能化

- 現在の4帯域ピーク方式を即座に削除せず、共通の検出器インターフェース経由で呼び出す構成へ変更する。
- 新しい方式と現在方式を同じベンチマークで比較できるようにする。
- DBには指紋方式名と指紋バージョンを保存し、異なる方式の指紋を区別する。
- DB移行時は、解析アプリで新指紋を生成し終えるまで現在方式の指紋を利用できる状態を維持する。
- 採用方式はライブラリ名だけで決めず、M0.5aの実測結果、CPU負荷、ライセンス、配布構成を含めて決定する。

### 8.4 M0.5c: BPM解析と候補優先度

- `AutoVJ.Catalog` で各登録曲の基準BPMを解析し、DBへ保存する。
- BPMには推定値、推定信頼度、自動解析値か手動指定値かを区別できる情報を持たせる。
- リアルタイム入力から現在BPMを推定し、`現在BPM / 基準BPM` をテンポ倍率候補として使用する。
- BPMは同値の曲や半分／2倍の誤認があるため、曲候補の完全除外には使わない。
- BPMは指紋照合で先に試す曲とテンポ倍率を絞る補助情報とし、最終確定は音声指紋で行う。
- 自動BPM解析の誤りに備え、後続工程で手動修正できる余地をDBに残す。

### 8.5 M0.5d: 時間軸倍率を考慮する音声指紋

- キーロックでは周波数関係が概ね維持され、時間軸だけが伸縮する前提で照合する。
- 対数スペクトログラムから時間・周波数方向の局所ピークを抽出し、ピーク間の関係をランドマーク指紋として保存する方式を第一候補とする。
- 照合では、登録曲上の時刻と入力時刻の間に `登録時刻 = 入力時刻 × テンポ倍率 + 開始位置` の関係が成立する候補を探す。
- BPM推定が安定している場合は推定倍率周辺を優先し、不安定な場合はM0.5aで定めた倍率範囲を探索する。
- 検出結果として曲、基準曲上の推定位置、信頼度、推定テンポ倍率を返す。
- EQやLINE経路の差に対しては、絶対的な音量より局所的なスペクトルピーク関係を優先して比較する。
- テンポとピッチが同時に変化する入力への周波数軸補正は、キーロック対応が安定した後の拡張とする。

### 8.6 M0.5e: 動画再生速度の追従

- 検出したテンポ倍率をWeb UIへ公開し、HTML Videoの `playbackRate` へ反映する。
- 基準BPM120の曲を入力BPM132で検出した場合、テンポ倍率と動画再生速度は `1.10` とする。
- 検出動画へ切り替えるときは、音声指紋から得た基準曲上の位置へシークしてからテンポ倍率を設定する。
- 同じ曲を再検出した場合は、推定位置差だけでなく現在の `playbackRate` と推定倍率の差も補正対象とする。
- クロスフェード中は新旧レイヤーの速度を個別に管理し、新しい動画へ推定倍率を設定する。
- 汎用動画へ戻る場合は `playbackRate` を `1.0` に戻す。
- Web UIには基準BPM、入力推定BPM、テンポ倍率、動画再生速度を表示する。

### 8.7 検出状態とフィルター解除後の復帰

- フィルター適用中やMIX中に信頼度が下がっても、現在の検出曲と動画をすぐには破棄しない。
- 新しい曲は一定以上の信頼度と連続確認を満たした時点で確定する。
- フィルター解除後の新曲確定時間をベンチマークで測定し、2秒以内を目標、難しい場合でも現在の4秒検出窓以内を許容値として調整する。
- フィルター適用中そのものの正解率より、解除後の誤検出なしと復帰速度を優先する。
- 複数曲が同時に鳴っている状態で後入り曲を優先する本格的なMIX判定は、M0.5のテンポ耐性確立後に実装する。

### 8.8 M0.5の実装順序と完了条件

1. M0.5aのベンチマークを実装し、現在方式の基準値を保存する。
2. 検出器インターフェースと指紋方式バージョンを導入する。
3. BPMの事前解析、DB保存、リアルタイム推定を追加する。
4. キーロック前提の時間軸倍率対応指紋を実装する。
5. 動画の `playbackRate` 追従とUI表示を追加する。
6. EQ解除後の復帰時間、通常速度の回帰、CPU負荷を実機で検証する。

M0.5の必須完了条件は次のとおりとする。

- 通常速度 `1.00` の既存検出精度を維持する。
- キーロックを維持した `0.93` から `1.15` の範囲を主要対応範囲とする。
- `0.90` と `1.17` は追加目標として結果を記録する。
- テンポ変更時も正しい曲と基準曲上の位置を返し、誤検出を増やさない。
- 推定テンポ倍率を動画の再生速度へ反映し、定期再検出で位置と倍率を補正できる。
- 各倍率の検出率、位置誤差、確定時間、処理時間を再現可能なベンチマーク結果として残す。

## 9. 次の実機確認

1. AG03MK2のLINE入力へ登録曲を流す。
2. Web UIで「入力開始」を押す。
3. 4〜5秒程度で曲名・信頼度・推定位置が確定するか確認する。
4. 動画が正しい位置から始まるか確認する。
5. 曲を途中でシークし、数回の照合後に動画が再同期するか確認する。
6. 2曲を切り替え、別動画へ切り替わるか確認する。
7. 音声を止め、10秒後に汎用動画へ戻ることを確認する。
8. Web UIで入力デバイスを切り替え、メーター表示が追従することを確認する。
